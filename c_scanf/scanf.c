#include <stdio.h>

//
int main(void) {
    // code ...
    int a;
    int b;
    while (scanf("%d %d", &a, &b) == 2) {

        printf("a=%d \n", a);
        printf("b=%d \n", b);
    }
    return 0;
}
